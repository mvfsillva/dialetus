import styled from 'styled-components'

const Main = styled.main`
  width: 100%;
  max-width: 1200px;
  padding: ${({theme}) => theme.spacing.xxxLarge};
  margin-left: auto;
  margin-right: auto;
`
export default Main
