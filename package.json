{
  "name": "dialetus",
  "version": "0.0.0",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "lint": "xo --quiet"
  },
  "peerDependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2"
  },
  "dependencies": {
    "prop-types": "^15.6.2"
  },
  "devDependencies": {
    "lint-staged": "^7.2.2",
    "xo": "^0.22.0"
  },
  "xo": {
    "extends": [
      "prettier",
      "prettier/react",
      "plugin:react/recommended"
    ]
  },
  "lint-staged": {
    "*.js": [
      "yarn test",
      "yarn lint",
      "prettier --semi false --single-quote --write",
      "git add"
    ]
  }
}
